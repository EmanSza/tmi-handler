module.exports = {
    // Events go by fileName instead of a defined name.
    // This is because the file name is the same as the event name.
        
    execute: async function() {
        console.log('working')
    }
}